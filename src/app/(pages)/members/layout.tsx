import React from 'react'
import { PageProps } from '../layout'

function MembersLayout({ children }: PageProps) {
    return (
        <div>
            {children}
        </div>
    )
}

export default MembersLayout
